{
  "name": "@prismhub/mcp-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transport-independent Prismhub MCP tool/resource/prompt registry.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit --tsBuildInfoFile typecheck.tsbuildinfo",
    "lint": "eslint src --max-warnings=0",
    "lint:fix": "eslint src --fix",
    "test": "bun test",
    "test:unit": "bun test unit.test",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@prismhub/contracts": "workspace:*",
    "@prismhub/core": "workspace:*",
    "@prismhub/observability": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
